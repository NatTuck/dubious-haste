llm:
  baseUrl: "http://kraken:8080/v1"
  model: "local-model"
  apiKey: "no-key"

judge:
  baseUrl: "http://kraken:8080/v1"
  model: "local-model"
  apiKey: "no-key"

benchmark:
  maxRounds: 20
  agentTimeout: 60
  maxRetries: 3
